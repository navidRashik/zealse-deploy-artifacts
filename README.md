# Zealse Connect deploy artifacts

Auto-built deploy archives + the server-side relay script used to deploy them
(GitHub Actions minutes were exhausted on the owner account; the relay runs as
a Hostinger cron job instead). Archives are build outputs of
ZEALVE-LTD/chatroom — see that repo for source.
